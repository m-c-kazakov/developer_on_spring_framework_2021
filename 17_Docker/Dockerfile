# это инструкция, которая задаёт родительский (главный) образ
# Oбраз создаётся на основе alpine linux с установленной openjdk11
FROM adoptopenjdk:11-jre-hotspot

# это то инструкция, которая устанавливает рабочую директорию для инструкции CMD и ENTRYPOINT
# Назначаем рабочую директорию, в которой будут выполняться дальнейшие команды (перемещаемся в папку app)
WORKDIR /opt/app

# это инструкция, которая определяет переменную для передачи Docker’у во время сборки
# Переменной JAR_FILE присваивается путь к jar- архиву
ARG JAR_FILE=build/libs/rest_js-0.0.1-SNAPSHOT-plain.jar

# это инструкция, которая копирует файлы и директории в контейнер
# Наш jar-файл, указанный в JAR_FILE, копируется в папку app, и копии задаётся имя app.jar
COPY ${JAR_FILE} app.jar

# это инструкция, которая предоставляет команды и аргументы для выполняющегося контейнера
ENTRYPOINT ["java","-jar","app.jar"]
